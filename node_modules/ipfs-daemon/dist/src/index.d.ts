export = Daemon;
declare class Daemon {
    /**
     * @param {import('ipfs-core').Options} options
     */
    constructor(options?: import('ipfs-core').Options);
    _options: import("ipfs-core/src/types").Options;
    /**
     * Starts the IPFS HTTP server
     */
    start(): Promise<void>;
    _ipfs: import("ipfs-core/src/components") | undefined;
    _httpApi: HttpApi | undefined;
    _httpGateway: HttpGateway | undefined;
    _grpcServer: import("ipfs-grpc-server/dist/src/types").WebsocketServer | undefined;
    stop(): Promise<void>;
}
import HttpApi = require("ipfs-http-server");
import HttpGateway = require("ipfs-http-gateway");
//# sourceMappingURL=index.d.ts.map